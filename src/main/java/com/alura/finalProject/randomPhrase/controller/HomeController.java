package com.alura.finalProject.randomPhrase.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.ResponseBody;

/*@Controller
public class HomeController {

    @GetMapping("/")
    @ResponseBody
    public String home() {
        return "Random Phrase API está funcionando! ✅<br>" +
                "Endpoints disponíveis:<br>" +
                "- GET /api/phrases (listar frases)<br>" +
                "- POST /api/phrases (criar frase)<br>" +
                "- GET /api/phrases/random (frase aleatória)";
    }
}*/
